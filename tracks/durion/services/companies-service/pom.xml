<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>tech.vlstech.durion</groupId>
  <artifactId>companies-service</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>companies-service</name>
  <description>
    Owns the Company aggregate (REQ-1). Governed by AGENTS.md, docs/adr/,
    and agents/services/companies-service.agent.md at the track root.
  </description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!--
      Schema migrations. Versioned SQL, not Hibernate auto-ddl — this is a
      cross-cutting convention that should apply to all three services once
      they have entities of their own, worth its own ADR (flagged
      separately, not yet written as of this REQ-1 implementation).
      Postgres support is a separate module as of Flyway 10.
    -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <!--
      Not managed by spring-boot-starter-parent's BOM, so pinned explicitly.
      Generates this service's OpenAPI contract from annotated code once
      REQ-1's endpoints exist — the producer side of ADR-0004's drift check
      against tracks/durion/services/companies-service/openapi.yaml.
    -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.6.0</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      In-memory database for the test suite only — CI's build-and-test job
      (matrix, per service) doesn't bring up a real Postgres, only
      compose-smoke-test does. Postgres-compatibility mode narrows the gap,
      but this is a pragmatic substitution, not a claim the two behave
      identically in every respect.
    -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
