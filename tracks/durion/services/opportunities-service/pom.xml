<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>tech.vlstech.durion</groupId>
  <artifactId>opportunities-service</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>opportunities-service</name>
  <description>
    Owns the Opportunity aggregate, pipeline stages, and automation
    triggers (REQ-3 through REQ-11). INFRA-3 scaffolding only — no
    business logic yet. Governed by AGENTS.md, docs/adr/, and
    agents/services/opportunities-service.agent.md at the track root.
  </description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!--
      Not managed by spring-boot-starter-parent's BOM, so pinned explicitly.
      Generates this service's OpenAPI contract from annotated code once
      REQ-3's endpoints exist — the producer side of ADR-0004's drift check
      against tracks/durion/services/opportunities-service/openapi.yaml.
    -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.6.0</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <!--
    Deliberately no spring-kafka dependency yet, even though this service
    will end up owning the ext_companies_company / ext_contacts_person
    consumers (ADR-0001) and the REQ-9 Closed Won publisher. INFRA-3 mirrors
    INFRA-1/INFRA-2's plain scaffold; Kafka wiring belongs to INFRA-6, per
    AGENTS.md's build order ("wire cross-service concerns after the
    services that produce/consume them exist").
  -->

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
